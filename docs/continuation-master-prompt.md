# FeedSight continuation master prompt

Continue the existing FeedSight AI repository. Inspect git status, current code,
datasets and tests first. Preserve working features and other contributors'
changes. Do not rebuild, reset, force-push or commit raw datasets/secrets.

Keep image classification, experimental biomass regression, nutrition reference
lookup and measured values separate. Inspect the actual model before claiming
classes or confidence. Never convert biomass grams into nutrient concentration.

Improve farmer usability with accessible, mobile-friendly interactive charts,
clear explanations, manual correction and honest empty states. Use recorded data
only. Keep simulated data labelled and separate. Never double-count biomass
totals, invent history, or display unsupported precision or predictions.

Extend the safety-first nutrient balance engine. Compare only compatible units
and genuinely documented benchmarks. Current ingredient minima are not validated
animal requirement profiles. Unknown values stay unknown; met minima do not
trigger unnecessary supplements. Keep safety and storage ahead of optimization.

Use sourced qualitative complement categories. Filter by animal context and
supported gaps; show why, source and limitations. Keep potential-support views
qualitative, with no assumed after-feeding composition, exact dose, replacement
claim, milk increase, fertility promise or profit guarantee. Add mineral or
energy recommendations only after appropriate reference data is validated.

Reuse existing measurements when switching goals. Preserve immutable saved
reports and normal Supabase synchronization. Keep offline operation and existing
languages/TTS. Never claim whole-ration adequacy from an individual ingredient.

Test unit compatibility, missing data, adequate values, multiple gaps, unsafe
feed, animal restrictions, goal switching, saved history, offline use, model
fallback and mobile interaction. Run the production build. Document actual
results and remaining data limitations.

When publishing is authorized, commit only reviewed changes, push to the
existing GitHub main without force, use the existing Vercel project, and verify
the resulting public application. Report the exact commit, verified production
URL, test outcomes and unresolved limitations. Never claim deployment succeeded
without checking the live result.
